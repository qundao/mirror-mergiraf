# Tutorial

Get started with:
```python
from bookmaker import front_page, compile

def main():
    compile(debug=True)
```

or in Java:
```java
Bookmaker bm = new Bookmaker();
bookmaker.compile(true);
```
